imports:
    - { resource: framework.yml }
    - { resource: twig.yml }
    - { resource: bundle.yml }
    - { resource: sensio_framework_extra.yml }

services:
    logger:
        class: Psr\Log\NullLogger
